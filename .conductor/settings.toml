"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "true"
run = 'PORT=${CONDUCTOR_PORT:-4173}; echo "http://localhost:$PORT"; ruby -run -e httpd . -p "$PORT"'
archive = "true"
run_mode = "concurrent"
